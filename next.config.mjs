/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "quaint-husky-315.convex.cloud"
        },
      ],
    },
  };
  
  export default nextConfig;
  
  